# Initial independent-review synthesis

The initial evidence packet at source
`9f78aef04c86c05cff14c76e2dfcac6e62986010` and tree
`481bd657db43b7fa683b0a2bd064bc3a378daad3` is rejected and retained in Git
history. Both fresh-context reviewers returned **REVISE** without seeing the
other scorecard. Scores were 83/100 and 78/100 overall; neither official gate
passed.

## Converged corrections for bounded revision cycle 1

- repair the Landing and Home forced-colors label loss and add a direct visual
  style regression;
- complete Authentication through `home/returning` and Home through
  `product/partial` in both normal and reduced recordings;
- eliminate residual PL/DE English and machine-state tokens;
- make Search `typing`/`suggestions` and Product `partial`/`degraded` distinct;
- attach source-matched per-reference performance evidence;
- recapture the complete typography assay;
- strengthen auth/search identity application and the small-size source mark.

Reviewer B's additional blocking corrections are also accepted: remove the
extra path glyph so the wordmark is exactly `TryVit`, and derive Product
provenance language from the active unknown/assessed state.

The initial accessibility and truthfulness vetoes remain open until both
reviewers inspect replacement evidence from a new exact source. This synthesis
does not approve the system and does not count as an independent review.
